# macOS metadata
.DS_Store

# Model files — download via install.sh
models/

# Per-user config — generated by install.sh
whisper-stt.conf

# Transcription history — personal data
history.txt

# Partial downloads
*.part
push-public.sh